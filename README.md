This is a arp spoofer code without UI, you can take this and add UI too. This script leverages the Scapy library to perform ARP spoofing by continuously sending forged ARP packets to both a specified target and the default gateway. By manipulating the ARP tables of these devices, it effectively intercepts and redirects their network traffic while ensuring the original network state is restored upon script termination.
